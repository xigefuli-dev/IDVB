using System.Runtime.Versioning;

// GenerateAssemblyInfo is disabled because IDVB generates its version attributes
// during the build. Preserve the platform attributes that the SDK would otherwise
// emit so platform compatibility analysis recognizes this as a Windows-only app.
[assembly: TargetPlatform("Windows10.0.19041.0")]
[assembly: SupportedOSPlatform("Windows10.0.17763.0")]
